<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.example.android.myapplication.ui.SmoothieMeActivity">

    <!-- LinearLayout for holding three SmoothieMeAndCo images -->
    <LinearLayout
        android:id="@+id/smoothie_me_linear_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@android:color/white"
        android:orientation="vertical"
        android:paddingBottom="16dp"
        android:paddingLeft="16dp"
        android:paddingRight="16dp"
        android:paddingTop="16dp">

        <!-- Container to hold the title SmoothiePartFragment of the custom android-->
        <!-- The container should be 180dp in height-->
        <FrameLayout
            android:id="@+id/title_container"
            android:layout_width="match_parent"
            android:layout_height="180dp"
            android:scaleType= "centerInside"/>

        <!-- The container should be 180dp in height-->
        <FrameLayout
            android:id="@+id/fruit_container"
            android:layout_width="match_parent"
            android:layout_height="180dp"
            android:scaleType= "centerInside"/>

        <!-- The container should be 180dp in height-->
        <FrameLayout
            android:id="@+id/vegetable_container"
            android:layout_width="match_parent"
            android:layout_height="180dp"
            android:scaleType= "centerInside"/>

    </LinearLayout>

</ScrollView>